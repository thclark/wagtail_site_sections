# These are the requirements for TESTING, not for the actual library. Useful for quick local TDD without tox
django==2.2.13
djangorestframework
mock==2.0.0
wagtail==2.3
wagtail-factories==1.1.0
tox==3.7.0
